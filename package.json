{
  "name": "docker_node_app",
  "version": "1.0.0",
  "description": "Nodejs on Docker",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "javy256",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
